# Site code assets — reference copies

The CMS database is the source of truth for these; Instatic stores them as
code assets and serves them at `/_instatic/assets/<build>/entries/<name>.js`.
The copies here exist so the logic is reviewable in git and restorable if a
database is lost. Edit them in the CMS, then refresh the copy here.
