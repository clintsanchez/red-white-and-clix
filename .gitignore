.env
CREDENTIALS.local.md
.DS_Store
__pycache__/
*.pyc
build/*.html

# The private onboarding repository intentionally retains memory/*.json.
# Never add live credentials or local secret files to memory/.

# Local browser logs and oversized duplicate bundle
.playwright-mcp/
deliverables/Red White and Clix Complete Design Kit.zip

# Local DB snapshots — contain admin user rows; never commit
backups/

# Plugin build artifacts; source lives in plugins/<name>/
*.plugin.zip

# Deploy seed snapshot: contains admin user rows. Rebuild with the documented
# docker cp commands; never commit it.
deploy/railway/seed/
deploy/railway/backups/uploads/
99-Reference/senator-banks-letter-ORIGINAL-unredacted.png
99-Reference/media/
